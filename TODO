



TODO:
	-Update the syntax of every variable from oldSyntax to new_syntax (ctrl+shift+h	to find all appearences (in all files) of a string, and change it like that)				(OPTIONAL)

alex:
	-Implement user pinging. If a user is not active he is removed from the user list (but still saved on the server's user file)	

	-Implement event-based user input (no particular order for the players)

maria:

